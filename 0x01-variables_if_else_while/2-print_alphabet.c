#include <stdio.h>
/**
 * main - Entery point 
 * followed by a new line
 *Return: Always 0 (if success)
 */
int main(void)
{
	char ka;

	for (ka = 'a'; ka <= 'z'; ka++);
	{
	putchar(ch);
	}
	putchar ('\n');
	return (0);
}
