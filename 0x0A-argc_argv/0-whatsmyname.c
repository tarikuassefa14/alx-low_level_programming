#include <stdio.h>
#include <stdlib.h>

/**
 *main - A program that prints its name, then a new line.
 *@argc: array length.
 *@argv: aragumen of an array.
 *Return: 0.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
