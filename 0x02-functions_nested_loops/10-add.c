#include "main.h"

/**
  * add - a function that adds two integers and returns the result
  * @a: is an integer
  * @b: is also integer
  *
  * Return: Always 0 (if success)
  */
int add(int a, int b)
{
	return (a + b);
}

