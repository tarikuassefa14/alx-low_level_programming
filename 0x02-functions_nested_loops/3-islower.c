#include "main.h"

/**
  * main - This is a program that print 10 times alphabet
  *
  * Return: Always 0 (if success)
  */
int _islower(int c)
{
	if (c >= 'a' &&	c <= 'z')
		return (1);
	else
		return (0);
}
