#include "main.h"

/**
  * main - This is a program that computes the absolute value of an integer
  *
  * Return: Always 0 (if success)
  */
int _abs(int k)
{
	if (k >= 0)
		return (k);
	else
		return (-k);
}
